# Frontend PrivScore

## Desarrollo
```powershell
npm install
npm run dev
```

## Build
```powershell
npm run build
```

El frontend usa `HashRouter` y `base` relativo en build para funcionar correctamente en GitHub Pages.
